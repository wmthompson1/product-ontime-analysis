CREATE TABLE [dbo].[NEXT_NUM_GEN_ETC] (
    [ROWID] int IDENTITY(1,1) DEFAULT  NOT NULL,
    [PROJECT_ID] NVARCHAR(15) DEFAULT  NOT NULL,
    [NEXT_NUMBER] int DEFAULT  NOT NULL
)

ALTER TABLE [dbo].[NEXT_NUM_GEN_ETC] ADD CONSTRAINT [PK_NEXT_NUM_GEN_ETC] PRIMARY KEY ([PROJECT_ID], [NEXT_NUMBER]);
