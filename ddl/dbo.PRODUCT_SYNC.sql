CREATE TABLE [dbo].[PRODUCT_SYNC] (
    [ROWID] int IDENTITY(1,1) DEFAULT  NOT NULL,
    [EVENT_ID] int DEFAULT  NOT NULL,
    [TYPE] NCHAR(1) DEFAULT  NOT NULL,
    [CREATE_DATE] datetime DEFAULT (getdate()) NOT NULL,
    [PRODUCT_CODE] NVARCHAR(15) DEFAULT  NOT NULL,
    [EXCEPTION] NCHAR(1) DEFAULT  NOT NULL,
    [SUBSCRIPTIONS] int DEFAULT ((0)) NOT NULL
)

ALTER TABLE [dbo].[PRODUCT_SYNC] ADD CONSTRAINT [PK_PRODUCT_SYNC] PRIMARY KEY ([EVENT_ID]);
