**34/34 passed.** Here's what was built in `test_sqlmesh_masking_policy.py`:

| Test class | Rule | What it covers |
|---|---|---|
| `TestRule1MaskingMatrix` (7) | Rule 1 | CSV exists, required columns, vendor root DAG 1.1, part→vendor ref |
| `TestRule2CertificateAttestation` (6) | Rule 2 | Cert fields, dag_lines ↔ matrix alignment, stale cert after adding row 1.3 |
| `TestRule3IngestionGate` (5) | Rule 3 | Valid cert passes; `status=pending/revoked`, empty dag_lines, mismatched dag_lines all block |
| `TestRule4Backfills` (4) | Rule 4 | Rebuild cascades vendor→part, backfill log grows, no writes on blocked run |
| `TestMaskingCorrectness` (8) | — | Faker-seeded vendor (20 rows) + part (30 rows); SHA-256 applied, non-PII fields untouched, deterministic, 64-char hex |
| `TestDAGIntegrity` (4) | — | No cycles, parent refs resolve, root identified as `vendor` |