**PR Description** that frames these 34 passing tests as the formal **Certification Proof** required for the 9:15 AM "Part" masking transition. This summarizes the move from Level 1.1 (Vendor) to Level 1.2 (Part) while ensuring the "Safe Zone" border crossing is technically verified.

---

## 📄 PR Description: Certification of Level 1.2 Masking (Part)

### **Executive Summary**
This PR elevates the masking logic from **DAG Level 1.1 (Vendor)** to **Level 1.2 (Part)**. It introduces the mandatory **Masking Matrix** as a governance gate within SQLMesh, ensuring that all downstream dependencies (like `part.pref_vendor`) are deterministically hashed to match their parent dimensions.

### **Certification Proof (34/34 Tests Passed)**
The following test classes verify the "Safe Zone" border crossing and policy compliance:

* **Rule 1: Matrix Integrity**: Confirms the `part → vendor` reference exists in the DAG and that the CSV contains all required columns for Level 1.1/1.2 execution.
* **Rule 2 & 3: Ingestion Gate**: Validates that only tables with active, non-stale certificates can pass from pre-stage to receiving. Mismatched or revoked `dag_lines` now correctly block ingestion.
* **Rule 4: Automated Backfills**: Proves that a change in the 8:00 AM matrix triggers a successful 9:15 AM rebuild of the `part` table, maintaining joinability with the `vendor` table.
* **Masking Correctness**: Verified 64-character SHA-256 hex application across 20 faker vendors and 30 faker parts; non-PII fields remain untouched.

### **Impact Assessment**
* **What Changed**: Added `pii_targets.csv` as a SQLMesh model dependency.
* **What Rebuilt**: `stg_vendors` (1.1) and `stg_parts` (1.2).
* **Governance**: SQLMesh plans now act as the central mechanism for certifying historical backfills against current privacy policies.

---

