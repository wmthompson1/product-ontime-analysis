
// src/pages/index.astro
---
// Component Script (runs at build time)
const title = "Hello World";
const message = "Welcome to Astro!";
const currentYear = new Date().getFullYear();
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <style>
      body {
        font-family: system-ui, sans-serif;
        margin: 0;
        padding: 2rem;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
      }
      .container {
        text-align: center;
        background: white;
        padding: 3rem;
        border-radius: 1rem;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
        max-width: 500px;
      }
      h1 {
        color: #333;
        margin-bottom: 1rem;
        font-size: 2.5rem;
      }
      p {
        color: #666;
        font-size: 1.2rem;
        margin-bottom: 2rem;
      }
      .button {
        background: #667eea;
        color: white;
        border: none;
        padding: 1rem 2rem;
        border-radius: 0.5rem;
        font-size: 1rem;
        cursor: pointer;
        transition: background 0.3s;
      }
      .button:hover {
        background: #5a67d8;
      }
      .footer {
        margin-top: 2rem;
        font-size: 0.9rem;
        color: #999;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>{title}</h1>
      <p>{message}</p>
      <button class="button" id="greetButton">Click me!</button>
      <div class="footer">
        Built with Astro • {currentYear}
      </div>
    </div>

    <script>
      // Client-side JavaScript
      document.getElementById('greetButton').addEventListener('click', () => {
        alert('Hello from Astro! 🚀');
      });

      // Add some interactive functionality
      const button = document.getElementById('greetButton');
      let clickCount = 0;

      button.addEventListener('click', () => {
        clickCount++;
        if (clickCount === 1) {
          button.textContent = '🎉 Clicked once!';
        } else if (clickCount === 5) {
          button.textContent = '🌟 You love clicking!';
        } else if (clickCount > 5) {
          button.textContent = `🚀 ${clickCount} clicks!`;
        }
      });
    </script>
  </body>
</html>

// src/components/Greeting.astro (Optional component example)
---
export interface Props {
  name: string;
}

const { name } = Astro.props;
---

<div class="greeting">
  <h2>Hello, {name}!</h2>
  <p>This is a reusable Astro component.</p>
</div>

<style>
  .greeting {
    border: 2px solid #667eea;
    border-radius: 0.5rem;
    padding: 1rem;
    margin: 1rem 0;
  }
  h2 {
    color: #667eea;
    margin: 0 0 0.5rem 0;
  }
  p {
    margin: 0;
    color: #666;
  }
</style>