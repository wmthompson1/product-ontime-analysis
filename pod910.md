All 5 todos done. Here's the full picture of what was built:

---

### What was created in `Utilities/SQLMesh/`

| File | Purpose |
|---|---|
| `scripts/generate_masking_seeds.py` | Faker generator — run to regenerate seeds; also prints fixture hashes for YAML sync |
| `seeds/vendor_seed.csv` | 20 faux vendor rows (UUID ids, company names, emails, country codes) |
| `seeds/part_seed.csv` | 30 faux part rows (FK → vendor ids, catch-phrase descriptions, buyer emails) |
| `models/raw/raw_vendor.sql` | SEED model — ingests `vendor_seed.csv`, declares columns + grain |
| `models/raw/raw_part.sql` | SEED model — ingests `part_seed.csv`, audits `NOT_NULL(pref_vendor)` |
| `models/staging/stg_vendor.sql` | FULL model — `SHA256(id) AS vendor_id`, DAG 1.1, `_dag_no` + `_masked_at` metadata |
| `models/staging/stg_part.sql` | FULL model — `SHA256(pref_vendor) AS pref_vendor_id`, inner-joins `stg_vendor` as the **certificate guard** (orphaned parts are excluded), DAG 1.2 |
| `tests/test_vendor_masking.yaml` | 2 YAML tests: single-row hash + two-row distinct-hash assertion |
| `tests/test_part_masking.yaml` | 2 YAML tests: FK hash matches vendor hash; orphaned part row excluded by inner join |

The `INNER JOIN` in `stg_part` is the physical enforcement of **Rule 3** — a part whose vendor hasn't passed the masking boundary simply doesn't make it through.