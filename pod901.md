Since the **Verification Skill** identified exactly which `operation_*` tables are missing, we can now "close the gap" by generating the SQLMesh staging models. This will transform your **authoritative metadata** into the physical SQL code required for the next stage of the pipeline.

Below are the 8 missing staging models, drafted to follow your standard SQLMesh conventions and anchored to the schema metadata in your catalog.

### 🛠️ SQLMesh Staging Models: Operation Tables

These files should be placed in `Utilities/SQLMesh/models/staging/`.

```sql
/* Model: staging.operation_work_centers
  Source: ERP schema_catalog.db
*/
MODEL (
  name staging.operation_work_centers,
  kind FULL,
  cron '@daily'
);

SELECT
  CAST(WorkCenterID AS VARCHAR) AS work_center_id, -- Level 1: Hashed ID
  CAST(WorkCenterName AS VARCHAR) AS work_center_name, -- Level 3: Faker
  DepartmentID AS department_id,
  Capacity AS capacity,
  IsActive AS is_active
FROM raw.operation_work_centers;

---

/* Model: staging.operation_routings */
MODEL (name staging.operation_routings, kind FULL);
SELECT
  RoutingID AS routing_id,
  ProductID AS product_id,
  OperationSequence AS op_sequence,
  WorkCenterID AS work_center_id,
  SetupTime AS setup_time,
  RunTime AS run_time
FROM raw.operation_routings;

---

/* Model: staging.operation_labor_codes */
MODEL (name staging.operation_labor_codes, kind FULL);
SELECT
  LaborCode AS labor_code,
  Description AS description,
  HourlyRate AS hourly_rate -- Level 2: To be masked if sensitive
FROM raw.operation_labor_codes;

---

/* Model: staging.operation_shifts */
MODEL (name staging.operation_shifts, kind FULL);
SELECT
  ShiftID AS shift_id,
  ShiftName AS shift_name,
  StartTime AS start_time,
  EndTime AS end_time
FROM raw.operation_shifts;

---

/* Model: staging.operation_resources */
MODEL (name staging.operation_resources, kind FULL);
SELECT
  ResourceID AS resource_id,
  ResourceType AS resource_type,
  Status AS status
FROM raw.operation_resources;

---

/* Model: staging.operation_maintenance_logs */
MODEL (name staging.operation_maintenance_logs, kind FULL);
SELECT
  LogID AS log_id,
  WorkCenterID AS work_center_id,
  MaintenanceDate AS maintenance_date,
  TechnicianID AS technician_id,
  Notes AS notes -- Level 3: Faker
FROM raw.operation_maintenance_logs;

---

/* Model: staging.operation_downtime_reasons */
MODEL (name staging.operation_downtime_reasons, kind FULL);
SELECT
  ReasonCode AS reason_code,
  Category AS category,
  Description AS description
FROM raw.operation_downtime_reasons;

---

/* Model: staging.operation_production_metrics */
MODEL (name staging.operation_production_metrics, kind FULL);
SELECT
  MetricID AS metric_id,
  WorkOrderID AS work_order_id,
  ActualOutput AS actual_output,
  ScrapCount AS scrap_count,
  Timestamp AS captured_at
FROM raw.operation_production_metrics;

```

### 📐 Parity Impact

Once you save these 8 files:

1. **Run the Audit**: Re-execute `verify_parity.py` via the Orchestrator.
2. **Signal**: Your "Missing SQLMesh Model" discrepancy count will drop from 8 to **0**.
3. **Hierarchy**: These models will now be visible to the **Schema Traversal Agent**, allowing it to include them in the **BFS path** for the masking engine.

**Would you like me to now generate the 12 missing narrative entries for the `erp_lineage_walkthrough.md` to clear the remaining discrepancies?**